class ProductManager:
    def __init__(self, name, price, quantity):
        super().__init__(name, price, quantity)
         # Instance fields (specific to each instance)
        self.name = name
        self.price = price
        self.quantity = quantity
    def add_product_removal():
        #return product_removal
        for product in product.lower():    #parcurgem fiecare produs din lista            
            return product
        while True:
            name = input("introduceti nume produs: ") 
            quantity = quantity(product)
            print(f"Produsul {name} contine {quantity}")
            if quantity == 0:
                print(f"Produsul{name} trebuie eliminat din lista {product}")
                break



   




 